@require '../markdown'

.area
  margin 6rem 0

  @extend .markdown

  +up-to(bp-medium)
    h2
      font-size 8vw

    h3
      font-size 6vw

    li, p
      font-size 4vw

    li:before
      top 3vw
